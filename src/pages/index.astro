---
import Base from '../layouts/Base.astro';
import Hero from '../components/Hero.astro';
import ImpactBand from '../components/ImpactBand.astro';
import PipelineDiagram from '../components/PipelineDiagram.astro';
import EngineStrip from '../components/EngineStrip.astro';
import ProjectCard from '../components/ProjectCard.astro';
import { site } from '../config';
import { getCollection } from 'astro:content';

const projects = (await getCollection('projects')).sort((a, b) => a.data.order - b.data.order);

// Curated headline impact (from real GTM work, genericised).
const headline = [
  { value: 'TAM tracking & enrichment', label: '' },
  { value: 'Lead scoring & prioritization', label: '' },
  { value: 'Outbound at scale', label: '' },
  { value: 'Enable data access', label: '' },
];
---
<Base title={`${site.name} · ${site.tagline}`}>
  <Hero />
  <ImpactBand metrics={headline} kind="capability" />
  <PipelineDiagram />
  <EngineStrip />
  <section id="work" class="wrap section">
    <div class="label">Selected work</div>
    <h2>Case studies.</h2>
    <div class="cards">
      {projects.map((p) => (
        <ProjectCard id={p.id} title={p.data.title} pitch={p.data.pitch}
          metric={p.data.metrics[0]} tech={p.data.tech} />
      ))}
    </div>
  </section>
  <section id="about" class="wrap section">
    <div class="label">About</div>
    <h2>How I work.</h2>
    <p class="sub" style="max-width:660px;color:var(--muted)">
      I'm a GTM engineer. I sit between the data team and the sales floor and build the machinery that
      connects them. Most of what's on this page started as a scrappy question from a rep ("why are we
      even working these accounts?") and turned into a model, a pipeline, or an n8n workflow that
      answered it for the whole team. I like the problems where the data science has to survive contact
      with a quota. If you've got a TAM you're not sure how to prioritise, or a GTM process still running
      on gut feel and copy-paste, that's exactly my kind of thing.
    </p>
    <p style="margin-top:20px"><a class="btn" href={site.calendly} target="_blank" rel="noopener">Book a 30-min call →</a></p>
  </section>
</Base>
<style>
  .cards{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-top:26px}
  @media (max-width:760px){ .cards{grid-template-columns:1fr} }
</style>
